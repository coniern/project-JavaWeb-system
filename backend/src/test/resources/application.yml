spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:projectdb-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: always
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    root: warn
